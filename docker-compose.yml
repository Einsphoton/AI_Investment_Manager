services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-investment
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/investment.db
    restart: unless-stopped
